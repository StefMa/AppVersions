{
  "version": 2,
  "builds": [
    {
      "src": "appVersionHelper.go",
      "use": "@now/go",
      "config": {
        "includeFiles": [
          "presentation/template/*.html"
        ]
      }
    },
    {
      "src": "favicon.ico",
      "use": "@vercel/static"
    }
  ],
  "routes": [
       { "src": "/", "dest": "appVersionHelper.go" }
   ]
}
